let selectedColors = ['red', 'blue']; 
console.log(selectedColors);

//in order to display specific elements within the array, you must write it in this form. Keep in mind 0 is always the first element.
console.log(selectedColors[0]); 

//You can also assign more elements to an already existing array like so.
selectedColors[2] = 'green';

// The elements within the array are dynamic i.e are able to change, also not all the elements need to be the same, you can have numbers and strings within the same array.