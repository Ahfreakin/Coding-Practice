const interestRate = 0.3;
console.log(interestRate);

// Const classifies as a constant value variable.
// Used for something that doesn't change.
//If you need to make a variable for something that changes, use "let" instead.